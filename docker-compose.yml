version: "2"

services:
  network-remote:
    image: gitlab.onlineterroir.com:5001/pitzzae/network-remote
#    image: network-remote
    container_name: network-remote
    restart: always
    network_mode: "host"
    volumes:
      - "./node/options.json:/data/remote/options.json"
    dns:
      - 10.2.1.244
      - 10.2.1.239